#
# Copyright (c) 2026 Nordic Semiconductor ASA
#
# SPDX-License-Identifier: LicenseRef-Nordic-5-Clause
#

# FILE_SUFFIX=provisioner replaces prj.conf and builds the provisioner/
# sub-application (provisioner_main.c) which calls provision_data().
# subsys/provisioner/prj.conf is merged via sysbuild.cmake add_overlay_config().

CONFIG_PROVISIONER=y
CONFIG_FP_PROVISION_DATA=y

CONFIG_SETTINGS=n
